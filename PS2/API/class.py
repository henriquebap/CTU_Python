#class ImageData:
 #   def __init__(self, image, metadata):
  #      self.image = image
   #     self.metadata = metadata
    ## Suponha que 'image1', 'image2', 'image3' sejam objetos de imagem e 'metadata1', 'metadata2', 'metadata3' sejam os metadados correspondentes.
    # #   image_data_list = [
    #  #      ImageData(image1, metadata1),
     #  #     ImageData(image2, metadata2),
      #  #    ImageData(image3, metadata3),
        #]
        #for image_data in image_data_list:
         #   imagem = image_data.image  # Acesse a imagem
          #  metadados = image_data.meta#data  # Acesse os metadados
        # Faça o que for necessário com #a imagem e os metadados
######